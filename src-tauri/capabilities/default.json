{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window basics plus the save and open dialogs. File writing is an application command, so it needs no permission here.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
